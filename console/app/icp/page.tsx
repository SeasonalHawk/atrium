// PRD v5 Section 8, "ideal customer profile editor": the profile that
// calibrates scoring and target generation, presented as an elicitation
// inventory (console/data/icp.questions.ts). Saving writes
// crew/config/icp.config.json, the single source the agents read.
export default function IcpEditorPage() {
  // TODO Phase 2: elicitation-driven ICP editor, writes icp.config.json.
  return null;
}
