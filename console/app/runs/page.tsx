// PRD v5 Section 8, "runs view": history of crew runs with command, lead,
// status, and timing, so the operator can audit what the crew has done.
export default function RunsPage() {
  // TODO Phase 2: query the Run table, render as a sortable/filterable list.
  return null;
}
