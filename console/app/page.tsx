// PRD v5 Section 6, "Operator console: pipeline board" — every lead and
// prospect grouped by LeadStage, with source, prospect score, and grade
// visible on each card. Selecting a card opens console/app/lead/[id].
// See the interactive model artifact (docs/) for the intended visual
// design before this is implemented against real Supabase data.
export default function PipelineBoardPage() {
  // TODO Phase 1: query Supabase leads via console/lib/supabase.ts, render
  // one column per LeadStage, refresh on load and after any run completes.
  return null;
}
