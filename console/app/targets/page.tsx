// PRD v5 Section 6, "Operator console: targets surface": add a target by
// company URL or generate a list from the saved ICP (console/data/target.questions.ts,
// console/app/icp), both through the shared elicitation engine. Launching a
// prospect run invokes /atrium prospect through the launcher.
export default function TargetsPage() {
  // TODO Phase 1: elicitation add-flow + pending/completed target list.
  return null;
}
